@model recenzent.Models.OrderReviewViewModel
@{
    ViewBag.Title = "OrderReview";
}
</div>
@using (Html.BeginForm("OrderReview", "AdminPanel", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
@Html.HiddenFor(m => m.pubId)
<div class="form-group col-md-10">
    <button type="submit" class="btn btn-default">Akceptuj</button>
</div>
}
</div>
@ViewBag.Message