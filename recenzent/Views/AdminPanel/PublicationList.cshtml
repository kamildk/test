@model PagedList.IPagedList<recenzent.Data.Model.Publication>
@using PagedList.Mvc;

@{
    ViewBag.Title = "PublicationList";
}

<h1>Lista Publikacji</h1>
<table>
    @foreach (var item in Model)
    {
        var tagList = item.PublicationTags.ToList();

        <tr>
            <td>
                <h2>
                    @Html.ActionLink(item.Title, "PublicationDetails", "AdminPanel", new { id = item.PublicationId }, null)
                </h2>
            </td>
    <tr>
       
        <td>
            <pre>@Html.DisplayFor(modelItem => item.Description)</pre>
        </td>
    </tr>
            <td >@Html.ActionLink(item.AuthoId, "UserDetails", "AdminPanel", new { id = item.AuthoId }, null)
            <br />
                @Html.ActionLink("Szczegóły", "PublicationDetai ls", "AdminPanel", new { id = item.PublicationId }, htmlAttributes: new { @class = "btn btn-default" })
               
            </td>
    <tr><td><img src="~/Images/pause.png" class="pause img-responsive"></td></tr>
        </tr>

    }
</table>
<br />
Strona @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) / @Model.PageCount