@model IEnumerable<recenzent.Data.Model.Publication>

@{
    ViewBag.Title = "Panel recenzenta";
}

<h2>Panel Recenzenta</h2>

<div class="container">
   @* 
    <div>
        <button type="button" class="btn" id="btn1" onclick="location.href='@Url.Action("Index", "AuthorPanel")'">Przejdź do panelu autora</button>
    </div>
   *@

    <div class="btn-default well-sm col-lg-11">
        @Html.ActionLink("Publikacje do zrecenzowania", "PublicationsToReview", "Reviewer")
    </div> 
    <div class="btn-default well-sm col-lg-11">
        @Html.ActionLink("Recenzje czekające na zatwierdzenie", "ReviewsWaiting", "Reviewer")
    </div>
    <div class="btn-default well-sm col-lg-11">
        @Html.ActionLink("Zatwierdzone recenzje", "ReviewsAccepted", "Reviewer")
    </div>

    @*<div class="list-group pre-scrollable" style="width:75%; min-height:750px" >
            @foreach (var item in Model)
            {
                <div class="list-group-item">
                    <h4 class="list-group-item-heading">@Html.ActionLink(item.Title, "PublicationReview", "ReviewerController", new { id = item.PublicationId }, null)</h4>
                </div>
            }
        </div>*@
</div>